package com.proyectoProgramacion.model
import androidx.annotation.DrawableRes
import androidx.annotation.StringRes
data class Persona(
    // es para segun yo hacer la lista scroleable (aun no se si funcione )
    val imageResourceId: Int
)
